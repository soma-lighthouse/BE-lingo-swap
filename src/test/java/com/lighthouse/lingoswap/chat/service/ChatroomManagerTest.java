package com.lighthouse.lingoswap.chat.service;

/*
@ExtendWith(SpringExtension.class)
@SpringBootTest ///이걸 안 넣으니까 아래 @Autowired 부분 에러 났다 -> 이유 알아보자
class ChatroomManagerTest {

    @Autowired EntityManager em;
    @Autowired ChatroomManager chatroomManager;

    @Test
    void create() {
        ChatroomRequest chatroomRequest = new ChatroomRequest();
        List<Long> members = null;
        chatroomRequest.setMembers(members);
        chatroomManager.create(chatroomRequest);
    }

    @Test
    void delete() {
    }
}*/
